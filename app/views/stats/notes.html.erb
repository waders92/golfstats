<div class="container top bottom">
  <h2>Notes About My Rounds</h2>
  <% current_user.notes.each do |note| %>
  <p class="info">
    <%= note.round.course%>  |  <%= note.created_at.strftime("%m/%d/%Y") %>:
    <%= note.message %><br />
  </p>
  <% end %>
</div>

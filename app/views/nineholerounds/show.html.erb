<div class="col-xs-10 col-xs-offset-1 recap bottom">
  <h1 class="text-center"><%= @nineholeround.course %></h1>
  <h4 class="text-center linkavg comment_handle"><%= @nineholeround.user.name %> | <%= @nineholeround.created_at.strftime("%m-%d-%Y") %><br /><br />
  <%= link_to 'Edit Round', edit_nineholeround_path(@nineholeround), class: 'btn btn-default' %>&nbsp;&nbsp;<%= link_to 'Delete Round', nineholeround_path(@nineholeround), class: 'btn btn-default', method: :delete, data: {confirm: 'Are you sure you want to delete this?'} %>
  </h4>
    <br /><br /><br />

  <div class="show-stats">
    <div class="col-xs-4 text-center big-score show-stats">
        <h4 class="comment_handle">Score</h4>
        <%= @nineholeround.score %>
    </div>

    <div class="col-xs-4 text-center big-score show-stats">
        <h4 class="comment_handle">Greens Hit</h4>
        <%= @nineholeround.greenshit %> / 9
    </div>

    <div class="col-xs-4 text-center big-score show-stats">
        <h4 class="comment_handle">Driving Accuracy</h4>
        <%= @nineholeround.fwys %> / <%= @nineholeround.fwystotal %>
    </div>
  </div>

  <div class="show-stats">
      <div class="col-xs-4 text-center big-score bottom">
        <h4 class="comment_handle">Putts</h4>
      <%= @nineholeround.putts %>
      </div>

      <div class="col-xs-4 text-center big-score bottom">
        <h4 class="comment_handle">Birdies</h4>
        <%= @nineholeround.birdies %>
      </div>

      <div class="col-xs-4 text-center big-score bottom">
        <h4 class="comment_handle">Pars</h4>
        <%= @nineholeround.pars %>
      </div>
  </div>
</div>
